# Hermes Admin 184→183 部署计划

**日期**: 2026-05-11
**版本**: v2026.05.10 → 183 测试环境
**状态**: 全量比对通过，Ingress 已修复，待执行

---

## 〇、检查清单

### 前置检查（部署前必须全部 ✅）

| # | 检查项 | 命令 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 1 | 183 Ingress hostPort 40080 可达 | `curl -s http://172.32.153.183:40080/admin/api/health` | `{"status":"ok"}` | ✅ 已验证 |
| 2 | 183 NodePort 32570 兼容可达 | `curl -s http://172.32.153.183:32570/admin/api/health` | `{"status":"ok"}` | ✅ 已验证 |
| 3 | 183 数据库 4 张共有表 schema 一致 | `kubectl exec postgres-0 -- psql -U hermes -d hermes_admin -c '\d'` | users/agent_skills/agent_metadata/skill_report_ids | ✅ 已验证 |
| 4 | 183 DATABASE_URL secret key 名一致 | `kubectl get secret hermes-database-secret -o jsonpath='{.data}'` | key `database-url` 存在 | ✅ 已验证 |
| 5 | 183 节点资源充足（1C/2G requests） | `kubectl describe node aidev03 \| grep Allocatable` | CPU ≥ 1C, Memory ≥ 2G 可用 | ✅ 96C/502G |
| 6 | 183 /data 磁盘空间充足 | `ssh root@183 df -h /data` | 可用 > 1G | ✅ 3.9T 可用 |
| 7 | 183 Ingress 路由规则与 184 一致 | `kubectl get ingress hermes-ingress -o yaml` | rewrite-target: /$2, use-regex: true | ✅ 已验证 |
| 8 | 184 镜像构建 BuildKit 上下文就绪 | `ls optional-skills/ tools/skills_hub_core.py` | 文件存在 | ⬜ 待验证 |
| 9 | 183 containerd 可用 | `/opt/containerd/bin/ctr -n k8s.io images ls` | 列出镜像 | ✅ 已验证 |

### 部署后验证（必须全部 ✅）

| # | 检查项 | 命令 | 预期结果 |
|---|--------|------|---------|
| 1 | Pod Running 1/1 | `kubectl get pods -n hermes-agent -l app=hermes-admin` | Running, 0 restarts |
| 2 | 健康检查 40080 | `curl http://172.32.153.183:40080/admin/api/health` | `{"status":"ok"}` |
| 3 | 前端页面加载 | `curl http://172.32.153.183:40080/admin/ \| head -5` | HTML 返回 |
| 4 | Admin 认证 | `curl -H "X-Admin-Key: $KEY" .../admin/api/agents` | Agent 列表 JSON |
| 5 | Skills Hub sources | `curl -H "X-Admin-Key: $KEY" .../admin/api/hub/sources` | source 列表 |
| 6 | DB 新表创建 | `kubectl exec postgres-0 -- psql -U hermes -d hermes_admin -c '\dt'` | 包含 agent_profiles, profile_templates, profile_audit_log |
| 7 | 资源设置生效 | `kubectl get pod ... -o jsonpath='{.spec.containers[0].resources}'` | requests 1C/2G, limits 2C/4G |
| 8 | 无硬编码密钥 | `kubectl get deploy ... -o yaml \| grep "value:" \| grep -v http` | 无敏感值 |
| 9 | hub-cache 目录 | `ls -la /data/hermes/hub-cache/` | 目录存在 |
| 10 | 921 条旧数据保留 | `kubectl exec postgres-0 -- psql -U hermes -d hermes_admin -c "SELECT count(*) FROM agent_skills"` | 921 条（或相近） |
| 11 | 兼容旧入口 32570 | `curl http://172.32.153.183:32570/admin/api/health` | `{"status":"ok"}` |

---

## 一、环境对比

### 基础设施

| 项目 | 184 (开发) | 183 (测试) |
|------|-----------|-----------|
| 节点 | roc-epyc | aidev03 |
| CPU/内存 | - | 96C / 502Gi (已用 15%/2%) |
| 磁盘 /data | - | 14T 总量, 3.9T 可用 |
| 容器运行时 | containerd (本地) | containerd (`/opt/containerd/bin/ctr`) |
| K8s 版本 | v1.28.2 | v1.28.2 |
| K8s 命名空间 | hermes-agent | hermes-agent |

### 网络架构

| 项目 | 184 | 183 |
|------|-----|-----|
| Ingress Controller | hostPort 40080/40443 | hostPort 40080/40443 ✅ 已修复 |
| 兼容入口 | - | NodePort 32570/30491 (旧入口保留) |
| WebUI 直连 | - | NodePort 31918 → hermes-webui:48080 |
| Admin Service | ClusterIP 10.108.99.148:48082 | ClusterIP 10.107.215.131:48082 |
| 外部 URL 前缀 | `http://172.32.153.184:40080` | `http://172.32.153.183:40080` |

#### 183 Ingress 路由表

##### hermes-ingress（主路由）

| 路径模式 | 后端 Service | 端口 | 行为 |
|----------|-------------|------|------|
| `/admin/api(/|$)(.*)` | hermes-admin | 48082 | rewrite → `/$2` |
| `/admin(/|$)(.*)` | hermes-admin | 48082 | rewrite → `/$2` |
| `/admin/assets/` | hermes-admin | 48082 | prefix (无 rewrite) |
| `/agent1(/|$)(.*)` | hermes-gateway-1 | 8642 | rewrite → `/$2` |
| ... | ... | ... | ... |
| `/agent13(/|$)(.*)` | hermes-gateway-13 | 8642 | rewrite → `/$2` |

Annotations: `rewrite-target: /$2`, `use-regex: true`, proxy timeout 60/120/120s

##### hermes-webui-ingress

| 路径 | 后端 Service | 端口 | 行为 |
|------|-------------|------|------|
| `/` | hermes-webui | 8080 | catch-all，无 rewrite |

Annotations: 仅 proxy timeout 60/120/120s（**无 gzip 配置，后续同步**）

##### Ingress 全量对比（183 vs 184）

| 项目 | 183 | 184 | 差异 |
|------|-----|-----|------|
| 主路由名称 | `hermes-ingress` | `hermes-ingress` | ✅ 相同 |
| WebUI 路由名称 | `hermes-webui-ingress` | `hermes-webui` | 名称不同 |
| rewrite-target | `/$2` | `/$2` | ✅ 一致 |
| use-regex | true | true | ✅ 一致 |
| proxy timeout | 60/120/120 | 60/120/120 | ✅ 一致 |
| admin 路径规则 | 3 条 | 3 条 | ✅ 一致 |
| gateway 路由数 | 13 条 | 4 条 | 环境差异 |
| webui gzip | ❌ 无 | ✅ 有 | ⚠️ 后续同步 |
| Ingress Controller | hostPort 40080 | hostPort 40080 | ✅ 已对齐 |

##### Ingress Controller 对齐修复（已完成 ✅）

183 原为 LoadBalancer Service（EXTERNAL-IP pending，降级 NodePort 32570）。已改为 hostPort 40080/40443，与 184 一致。

```
修改前: Client → 183:32570 (NodePort) → ingress-nginx → admin:48082
修改后: Client → 183:40080 (hostPort) → ingress-nginx → admin:48082
兼容旧: Client → 183:32570 (NodePort) → ingress-nginx → admin:48082 (仍然可用)
```

##### 183 网络策略

| 策略 | 目标 | 入站规则 |
|------|------|---------|
| `gateway-isolation` | gateway pods | 允许 8642/9119 端口 |
| `hermes-orchestrator` | orchestrator | 仅允许 admin pods 访问 8080 |
| `hermes-redis-netpol` | redis | 仅允许 admin/orchestrator/gateway 访问 6379 |
| `sandbox-isolation` | sandbox | 允许 hermes-agent namespace 访问 8642 |

### 运行中的服务 (183)

| 组件 | 副本数 | 年龄 |
|------|--------|------|
| hermes-admin | 1 | 16d |
| hermes-gateway-1~13 | 13 | 10-12d |
| hermes-orchestrator | 1 | 4d13h |
| hermes-redis | 1 | 8d |
| hermes-webui | 1 | 16d |
| postgres (StatefulSet) | 1 | 16d |

### 资源配额

| 项目 | 183 当前 | 部署后 |
|------|---------|--------|
| Admin requests | 100m CPU / 128Mi | **1C / 2Gi** |
| Admin limits | 500m CPU / 512Mi | **2C / 4Gi** |
| 节点 CPU 占用 | 14,400m (15%) | 15,400m (16%) |
| 节点内存占用 | 14,774Mi (2%) | 16,806Mi (3%) |

---

## 二、数据库详情

### 连接信息

| 项目 | 183 | 184 |
|------|-----|-----|
| Secret 名称 | `hermes-database-secret` | `postgres-secret` |
| Key 名称 | `database-url` | `database-url` ✅ 一致 |
| 连接串 | `postgresql+asyncpg://hermes:hermes2024@postgres:5432/hermes_admin` | (不同) |

### 表结构全量对比

#### 共有表（4 张，逐列比对完全一致 ✅）

**users** (16 列) — id, email, password_hash, display_name, agent_id, is_active, is_admin, created_at, updated_at, webui_jwt, webui_jwt_expires_at, webui_user_id, webui_password, provisioning_status, provisioning_error, provisioning_updated_at

**agent_skills** (9 列) — id, agent_number, skill_name, description, version, tags, skill_dir, reported_at, content_hash

**agent_metadata** (13 列) — agent_number, display_name, tags, role, description, updated_at, domain, skills, cpu_request, cpu_limit, memory_request, memory_limit

**skill_report_ids** (5 列) — report_id, agent_number, skills_count, tags_aggregated, processed_at

#### 184 独有表（3 张，183 不存在，启动时自动创建）

**agent_profiles** (12 列) — FK→profile_templates, UNIQUE(agent_number, profile_name), CHECK(sync_status)

**profile_templates** (10 列) — UNIQUE(name), 被 agent_profiles 引用

**profile_audit_log** (8 列) — 审计日志，索引(entity_type, entity_id)

迁移顺序: profile_templates → agent_profiles → profile_audit_log（因 FK 依赖）

### 现有数据

| 表名 | 行数 | 说明 |
|------|------|------|
| `users` | 10 | 用户账户 |
| `agent_skills` | 921 | 技能记录（gateway 上报的，部署后保留） |
| `skill_report_ids` | - | 幂等性检查（可废弃） |
| `agent_metadata` | - | Agent 元数据 |

### 其他 Secrets (183)

| Secret | Keys |
|--------|------|
| `hermes-admin-secret` | `admin_key` |
| `hermes-admin-internal-secret` | `admin_internal_token` |
| `hermes-database-secret` | `database-url` |
| `hermes-db-secret` | `api_key`, `api_key_2`, `api_key_3`, `password`, `username` |
| `hermes-redis-secret` | `redis-password` (+1 more) |
| `hermes-gateway-1~13-secret` | 各 1 key |
| `hermes-orchestrator-secret` | 1 key |
| `postgres-secret` | `database-url`, `password` (不同密码!) |

---

## 三、存储详情

### /data/hermes/ 目录结构 (183)

```
/data/hermes/              ← admin pod 挂载此目录
├── agent1/     (54M)
├── agent2/     (638M)
├── agent3/     (50M)
├── ...        (见详细列表)
├── agent13/    (45M)
├── _admin/     (8K)
├── _backups/   (652K)
└── webui/      (67M)
总计: ~2.4 GB
```

### 需要创建的目录

- `/data/hermes/hub-cache/` — Skills Hub 缓存目录（**部署前创建**）
- `/data/hermes/optional-skills/` — **不需要**（已烘焙进 Docker 镜像）

### PV/PVC (183)

| PV | 容量 | 绑定到 | 存储类 |
|----|------|--------|--------|
| hermes-data-pv | 50Gi | hermes-agent/hermes-data-pvc | local-storage |
| hermes-redis-pv | 5Gi | hermes-agent/hermes-redis-pvc | local-storage |

注: admin pod 不使用 PVC，直接用 hostPath `/data/hermes`。Redis 使用 PVC。

---

## 四、环境变量对比

### 需要变更的 env vars

| 变量 | 183 当前 | 183 目标 | 变更类型 |
|------|---------|---------|---------|
| `DATABASE_URL` | secretRef `hermes-database-secret` | 保持 `hermes-database-secret` | ⚠️ 184 yaml 用 `postgres-secret`，需改 |
| `EXTERNAL_URL_PREFIX` | `http://172.32.153.183:32570` | `http://172.32.153.183:40080` | 端口更新 |
| `EXTERNAL_WEBUI_URL` | `http://172.32.153.183:32570` | `http://172.32.153.183:40080` | 端口更新 |
| `EXTERNAL_API_BASE` | `http://172.32.153.183:32570` | `http://172.32.153.183:40080` | 端口更新 |
| `ADMIN_CORS_ORIGINS` | `http://172.32.153.183:32570` | `http://172.32.153.183:40080` | 端口更新 |
| `HERMES_OPTIONAL_SKILLS` | (缺失) | `/app/optional-skills` | 新增 |
| `HUB_CACHE_DIR` | (缺失) | `/data/hermes/hub-cache` | 新增 |
| `GITHUB_TOKEN` | (缺失) | secretRef `hub-github-token` (optional) | 新增 |
| `WEBUI_ADMIN_EMAIL` | 硬编码 `rocju315@gmail.com` | secretRef `hermes-admin-secret` | 迁移到 secret |
| `WEBUI_ADMIN_PASSWORD` | 硬编码 `HermesAdmin2026!` | secretRef `hermes-admin-secret` | 迁移到 secret |

### 184 yaml 中没有但 183 需要新增的变量（仅 2 个）

```yaml
# 183 的 Redis 需要密码认证，184 没有
- name: SWARM_REDIS_URL
  value: "redis://:$(REDIS_PASSWORD)@hermes-redis:6379/0"
- name: REDIS_PASSWORD
  valueFrom:
    secretKeyRef: { name: hermes-redis-secret, key: redis-password }
```

### 两环境都有但值/来源不同的变量

| 变量 | 184 | 183 目标 | 差异说明 |
|------|-----|---------|---------|
| `ORCHESTRATOR_API_KEY` | 硬编码 `507e56b...` | 硬编码 `94a975b...` | 密钥值不同，都硬编码 |
| `ADMIN_INTERNAL_TOKEN` | 硬编码 `hermes-internal-dev...` | secretRef `hermes-admin-internal-secret` | 183 更安全（用 secret） |
| `DATABASE_URL` | secretRef `postgres-secret` | secretRef `hermes-database-secret` | secret 名不同 |
| `WEBUI_INTERNAL_URL` | `http://hermes-webui:8080` | `http://hermes-webui:8080` | ✅ 一致 |
| `ORCHESTRATOR_INTERNAL_URL` | `http://hermes-orchestrator:8080` | `http://hermes-orchestrator:8080` | ✅ 一致 |

---

## 五、部署步骤

### Phase 0: 备份（在 183 上）

```bash
ssh root@172.32.153.183

# 备份当前 deployment
kubectl get deployment hermes-admin -n hermes-agent -o yaml > /tmp/hermes-admin-deployment-backup-20260511.yaml

# 备份 secrets
kubectl get secret hermes-admin-secret -n hermes-agent -o yaml > /tmp/hermes-admin-secret-backup.yaml
kubectl get secret hermes-database-secret -n hermes-agent -o yaml > /tmp/hermes-database-secret-backup.yaml
```

### Phase 1: 构建镜像（在 184 上）

```bash
cd /mnt/disk01/workspaces/worksummary/hermes-agent/admin
DOCKER_BUILDKIT=1 docker build \
  -f backend/Dockerfile \
  --build-context tools=../tools \
  --build-context optional-skills=../optional-skills \
  -t hermes-admin:2026.05.11 \
  .
```

### Phase 2: 传输镜像

```bash
# 在 184 上
docker save hermes-admin:2026.05.11 | gzip > /tmp/hermes-admin-2026.05.11.tar.gz
scp /tmp/hermes-admin-2026.05.11.tar.gz root@172.32.153.183:/tmp/

# 在 183 上
gunzip -c /tmp/hermes-admin-2026.05.11.tar.gz | /opt/containerd/bin/ctr -n k8s.io images import -
/opt/containerd/bin/ctr -n k8s.io images ls | grep hermes-admin
```

### Phase 3: 准备 183 Secrets（在 183 上）

```bash
# 3a. 更新 hermes-admin-secret，添加 webui-admin-email 和 webui-admin-password
kubectl patch secret hermes-admin-secret -n hermes-agent -p '{
  "data": {
    "webui-admin-email": "'$(echo -n "rocju315@gmail.com" | base64)'",
    "webui-admin-password": "'$(echo -n "HermesAdmin2026!" | base64)'"
  }
}'

# 3b. 创建 hub-github-token secret (空值，optional)
kubectl apply -f - <<EOF
apiVersion: v1
kind: Secret
metadata:
  name: hub-github-token
  namespace: hermes-agent
type: Opaque
stringData:
  github-token: ""
EOF
```

### Phase 4: 创建目录（在 183 上）

```bash
mkdir -p /data/hermes/hub-cache
chmod 777 /data/hermes/hub-cache
```

### Phase 5: 准备 183 专用 deployment yaml

```yaml
# 183 专用 deployment 的 env 段：
env:
  # === 通用 ===
  - name: K8S_NAMESPACE
    value: "hermes-agent"
  - name: ADMIN_KEY
    valueFrom:
      secretKeyRef: { name: hermes-admin-secret, key: admin_key }
  - name: HERMES_DATA_ROOT
    value: "/data/hermes"
  - name: PYTHONUNBUFFERED
    value: "1"

  # === 183 IP/端口（统一 40080）===
  - name: EXTERNAL_URL_PREFIX
    value: "http://172.32.153.183:40080"
  - name: ADMIN_CORS_ORIGINS
    value: "http://172.32.153.183:40080"
  - name: EXTERNAL_WEBUI_URL
    value: "http://172.32.153.183:40080"
  - name: EXTERNAL_API_BASE
    value: "http://172.32.153.183:40080"

  # === 183 secret 名不同 ===
  - name: DATABASE_URL
    valueFrom:
      secretKeyRef: { name: hermes-database-secret, key: database-url }

  # === Skills Hub 新增 ===
  - name: HERMES_OPTIONAL_SKILLS
    value: "/app/optional-skills"
  - name: HUB_CACHE_DIR
    value: "/data/hermes/hub-cache"
  - name: GITHUB_TOKEN
    valueFrom:
      secretKeyRef: { name: hub-github-token, key: github-token, optional: true }

  # === 183 专有（仅 2 个）===
  - name: SWARM_REDIS_URL
    value: "redis://:$(REDIS_PASSWORD)@hermes-redis:6379/0"
  - name: REDIS_PASSWORD
    valueFrom:
      secretKeyRef: { name: hermes-redis-secret, key: redis-password }

  # === 两环境都有但值不同 ===
  - name: WEBUI_INTERNAL_URL
    value: "http://hermes-webui:8080"
  - name: ORCHESTRATOR_INTERNAL_URL
    value: "http://hermes-orchestrator:8080"
  - name: ORCHESTRATOR_API_KEY
    value: "94a975bd887978ac63b177e5b9f158d56dfff69cb82dadb67647c24d2a27f930"
  - name: ADMIN_INTERNAL_TOKEN
    valueFrom:
      secretKeyRef: { name: hermes-admin-internal-secret, key: admin_internal_token }

  # === 密码迁移到 secret ===
  - name: WEBUI_ADMIN_EMAIL
    valueFrom:
      secretKeyRef: { name: hermes-admin-secret, key: webui-admin-email }
  - name: WEBUI_ADMIN_PASSWORD
    valueFrom:
      secretKeyRef: { name: hermes-admin-secret, key: webui-admin-password }
```

### Phase 6: 部署

```bash
kubectl apply -f /tmp/hermes-admin-183.yaml
kubectl rollout status deployment/hermes-admin -n hermes-agent --timeout=120s
```

### Phase 7: 验证

```bash
# 7a. Pod 状态
kubectl get pods -n hermes-agent -l app=hermes-admin

# 7b. 健康检查（新端口 40080）
curl -s http://172.32.153.183:40080/admin/api/health

# 7c. 前端页面
curl -s http://172.32.153.183:40080/admin/ | head -20

# 7d. Admin 认证
ADMIN_KEY=$(kubectl get secret hermes-admin-secret -n hermes-agent -o jsonpath='{.data.admin_key}' | base64 -d)
curl -s -H "X-Admin-Key: $ADMIN_KEY" http://172.32.153.183:40080/admin/api/agents | python3 -m json.tool

# 7e. Skills Hub
curl -s -H "X-Admin-Key: $ADMIN_KEY" http://172.32.153.183:40080/admin/api/hub/sources

# 7f. 资源设置
kubectl get pod -n hermes-agent -l app=hermes-admin -o jsonpath='{.items[0].spec.containers[0].resources}' | python3 -m json.tool

# 7g. DB 新表
kubectl exec -n hermes-agent postgres-0 -- psql -U hermes -d hermes_admin -c '\dt'

# 7h. 旧数据保留
kubectl exec -n hermes-agent postgres-0 -- psql -U hermes -d hermes_admin -c "SELECT count(*) FROM agent_skills"

# 7i. 兼容旧入口
curl -s http://172.32.153.183:32570/admin/api/health

# 7j. hub-cache 目录
ls -la /data/hermes/hub-cache/
```

---

## 六、回滚方案

```bash
# 快速回滚（在 183 上）
kubectl apply -f /tmp/hermes-admin-deployment-backup-20260511.yaml
kubectl rollout status deployment/hermes-admin -n hermes-agent --timeout=120s
```

旧镜像仍在 containerd 中，回滚无需重新传输。

---

## 七、风险矩阵

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| 1 | DATABASE_URL secret 名不匹配 | 🟢 已解决 | key 名一致，仅 secret 名不同 |
| 2 | DB schema 不兼容 | 🟢 无风险 | 4 张共有表逐列比对完全一致；3 张新表自动迁移 |
| 3 | DB 新表 FK 依赖 | 🟢 无风险 | 迁移顺序: profile_templates → agent_profiles → audit_log |
| 4 | 节点资源不足 | 🟢 无风险 | 96C/502G，升级后仅占 16%/3% |
| 5 | 184 yaml 缺少 183 专有 env | 🟡 中 | 仅 SWARM_REDIS_URL 和 REDIS_PASSWORD 需手动添加 |
| 6 | 硬编码密码残留 | 🟡 中 | ORCHESTRATOR_API_KEY 仍硬编码（后续优化） |
| 7 | 镜像传输失败 | 🟢 低 | gzip 后约 500MB，scp 即可 |
| 8 | Ingress 路由冲突 | 🟢 无风险 | rewrite-target/use-regex/timeout 完全一致 |
| 9 | Ingress webui gzip 缺失 | 🟢 低 | 后续同步 |
| 10 | optional-skills 不在 183 主机 | 🟢 无风险 | 已烘焙进镜像 |
| 11 | 921 条 agent_skills 数据 | 🟢 无风险 | 新代码兼容旧数据 |
| 12 | Ingress hostPort 端口冲突 | 🟢 已解决 | 40080/40443 空闲，已部署 |

---

## 八、建议的后续优化（非本次部署范围）

1. **ORCHESTRATOR_API_KEY 迁移到 Secret** — 当前硬编码
2. **GitHub Token 配置** — 填入实际 token，解除 60 req/hr 限制
3. **Kustomize overlay** — 为 183/184 创建不同 overlay，避免手动维护两份 deployment
4. **DB 迁移自动化** — 启动时自动迁移，建议加 pre-hook 验证
5. **TLS 配置** — 183 有 HTTPS hostPort 40443 但无证书
6. **WebUI Ingress gzip** — 同步 184 的 gzip 配置到 183
7. **Ingress Controller Service 清理** — 183 的 LoadBalancer Service 可降级为 ClusterIP（hostPort 已承担外部访问）
