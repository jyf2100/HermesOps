interface:
  display_name: "HermesOps"
  short_description: "Repo-specific patterns and workflows for HermesOps"
  default_prompt: "Use the HermesOps repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true